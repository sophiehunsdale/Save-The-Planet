package game;

/**
 * 
 * This enum is to ensure only valid types of Saving Field are used throughout
 * the game.
 * 
 * Future game developments would allow additional Saving Field types to be
 * added here.
 *
 */


public enum SavingFieldTypes {
	REDUCE, REBUILD, RECYCLE, RETHINK;
}
